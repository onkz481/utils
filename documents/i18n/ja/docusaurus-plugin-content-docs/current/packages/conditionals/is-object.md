---
title: isObject
sidebar_position: 7
---

`isObject`メソッドは、渡された値が`Object`であるかどうかを判別します。

```js
isObject({}); // true

isObject([]); // false
```

## 使い方

```js
import { isObject } from '@onkz481/conditionals';

isObject(value);
```
